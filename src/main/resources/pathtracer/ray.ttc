:ray
def new() = {
    origin = vec3(),
    direction = vec3()
}

def new(origin, direction) = {
    origin = origin,
    direction = direction
}

def at(self, t) = vec3:add(self.origin, vec3:nmul(t, self.direction))
:ray
